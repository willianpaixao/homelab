---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: longhorn-certs
spec:
  secretName: longhorn-certs
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  commonName: "longhorn.turing.${SECRET_DOMAIN}"
  dnsNames:
    - "longhorn.turing.${SECRET_DOMAIN}"
