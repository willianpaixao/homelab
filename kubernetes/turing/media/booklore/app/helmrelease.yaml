---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app booklore
  namespace: &namespace media
spec:
  interval: 1h
  timeout: 15m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 2
  test:
    enable: false
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      strategy: rollback
      retries: 3
  values:
    controllers:
      booklore:
        annotations:
          reloader.stakater.com/auto: "true"
        pod:
          securityContext:
            fsGroup: 2000
            fsGroupChangePolicy: "OnRootMismatch"
        containers:
          app:
            image:
              repository: ghcr.io/booklore-app/booklore
              tag: v1.18.4@sha256:636cdafcb29277083c6590a4dd3b451a7e7efa76526b19d363cecfeedbb156df
            env:
              TZ: ${TIMEZONE}
              PUID: "2000"
              PGID: "2000"
              DATABASE_URL: "jdbc:mariadb://localhost:3306/booklore"
              DATABASE_USERNAME:
                valueFrom:
                  secretKeyRef:
                    name: booklore-secret
                    key: DATABASE_USERNAME
              DATABASE_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: booklore-secret
                    key: DATABASE_PASSWORD
            probes:
              liveness: &probes
                enabled: true
                custom: true
                spec:
                  tcpSocket:
                    port: &port 6060
                  initialDelaySeconds: 30
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness: *probes
            securityContext:
              allowPrivilegeEscalation: true
            resources:
              requests:
                cpu: 100m
                memory: 256Mi
          mariadb:
            image:
              repository: mariadb
              tag: 11.8@sha256:345fa26d595e8c7fe298e0c4098ed400356f502458769c8902229b3437d6da2b
            args:
              - --wait_timeout=600
              - --interactive_timeout=600
              - --innodb_snapshot_isolation=0
            env:
              MYSQL_ROOT_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: booklore-secret
                    key: MYSQL_ROOT_PASSWORD
              MYSQL_DATABASE: booklore
              MYSQL_USER:
                valueFrom:
                  secretKeyRef:
                    name: booklore-secret
                    key: DATABASE_USERNAME
              MYSQL_PASSWORD:
                valueFrom:
                  secretKeyRef:
                    name: booklore-secret
                    key: DATABASE_PASSWORD
            probes:
              liveness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command:
                      - mariadb-admin
                      - ping
                      - -h
                      - localhost
                      - -uroot
                      - -p$(MYSQL_ROOT_PASSWORD)
                  initialDelaySeconds: 30
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
              readiness:
                enabled: true
                custom: true
                spec:
                  exec:
                    command:
                      - mariadb-admin
                      - ping
                      - -h
                      - localhost
                      - -uroot
                      - -p$(MYSQL_ROOT_PASSWORD)
                  initialDelaySeconds: 10
                  periodSeconds: 10
                  timeoutSeconds: 5
                  failureThreshold: 3
            resources:
              requests:
                cpu: 50m
                memory: 128Mi
    service:
      app:
        controller: *app
        ports:
          http:
            port: *port
    ingress:
      app:
        className: internal
        annotations:
          gethomepage.dev/enabled: "true"
          gethomepage.dev/group: Media
          gethomepage.dev/icon: book-lore.png
          gethomepage.dev/name: Booklore
        hosts:
          - host: &host booklore.${SECRET_DOMAIN}
            paths:
              - path: /
                service:
                  identifier: app
                  port: http
        tls:
          - hosts:
              - *host
    persistence:
      config:
        enabled: true
        type: persistentVolumeClaim
        accessMode: ReadWriteOnce
        size: 1Gi
        advancedMounts:
          booklore:
            app:
              - path: /app/data
      books:
        enabled: true
        type: nfs
        server: ${NFS_SERVER}
        path: /books
        advancedMounts:
          booklore:
            app:
              - path: /books
      bookdrop:
        enabled: true
        type: emptyDir
        advancedMounts:
          booklore:
            app:
              - path: /bookdrop
      tmp:
        enabled: true
        type: emptyDir
        advancedMounts:
          booklore:
            app:
              - path: /tmp
      db:
        enabled: true
        type: persistentVolumeClaim
        storageClass: longhorn
        accessMode: ReadWriteOnce
        size: 5Gi
        advancedMounts:
          booklore:
            mariadb:
              - path: /var/lib/mysql
    defaultPodOptions:
      labels:
        app: booklore
        environment: production
        owner: willianpaixao
